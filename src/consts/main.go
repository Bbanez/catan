package consts

import "math"

const (
	Pi14 = math.Pi / 4.0
	Pi12 = math.Pi / 2.0
	Pi13 = math.Pi / 3.0
	Pi32 = (3.0 * math.Pi) / 2.0
	Pi34 = (3.0 * math.Pi) / 4.0
	Pi54 = (5.0 * math.Pi) / 4.0
	Pi74 = (7.0 * math.Pi) / 4.0
)
